#!/usr/bin/make -f
%:
	dh $@

override_dh_strip:
	# dh_strip is skipped because objcopy is failing on the flm binary

override_dh_dwz:
	# dh_dwz is skipped because it causes issues with the binary structure

override_dh_auto_configure:
	cmake -S src -B build --preset linux-default

override_dh_auto_build:
	cmake --build build

override_dh_auto_install:
	DESTDIR=$(CURDIR)/debian/fastflowlm cmake --install build --prefix=/opt/fastflowlm
	rmdir --ignore-fail-on-non-empty debian/fastflowlm/opt/fastflowlm/bin
	install -d debian/fastflowlm/opt/fastflowlm debian/fastflowlm/usr/bin
	ln -sf /opt/fastflowlm/bin/flm debian/fastflowlm/usr/bin/flm

	# Remove include directories
	rm -rf debian/fastflowlm/opt/fastflowlm/include

override_dh_shlibdeps:
	# The NPU engine .so and flm link the bundled libhrx (and the private engine
	# libs), which are shipped inside this package rather than provided by another
	# .deb. Point dpkg-shlibdeps at the bundled lib dirs so it can locate them, and
	# ignore the missing external dependency info for those private libraries.
	# dh_shlibdeps treats -l as a single value, so both directories must be passed
	# in one colon-separated argument; otherwise only the last -l is honoured and
	# dpkg-shlibdeps never searches lib/ (where libhrx.so.0 lives).
	dh_shlibdeps \
	  -l$(CURDIR)/debian/fastflowlm/opt/fastflowlm/lib:$(CURDIR)/debian/fastflowlm/opt/fastflowlm/lib/flm \
	  -- --ignore-missing-info
